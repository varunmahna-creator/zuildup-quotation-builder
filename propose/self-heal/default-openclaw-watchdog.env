# /etc/default/ocplatform-watchdog
# Mode: 0640 root:root (root-readable; sourced by watchdog script).
# Get the webhook URL from Discord channel: #dhurandhar-iraaj or whatever ops channel.

DISCORD_WEBHOOK_URL=
